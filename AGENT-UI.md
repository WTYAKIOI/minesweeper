# AGENTS-UI.md – 扫雷认知助手前端界面设计

## 1. 概述

本文档为 **扫雷认知助手 (Minesweeper Cognitive Agent)** 的前端界面提供设计规范与交互蓝图。界面需要同时支持 **手动棋盘编辑** 与 **OCR 自动识别**，并清晰展示推理结果、引导对话和策略建议。

核心设计目标：
- **极简** – 减少视觉噪音，专注棋盘与推理链
- **纯色背景** – 使用单一背景色，营造沉浸感
- **信息分层** – 将棋盘、推理结果、对话区自然分隔，便于阅读
- **零门槛交互** – 支持对话框直接粘贴截图或文本棋盘，无需额外操作菜单

---

## 2. 设计原则

| 原则 | 说明 |
|------|------|
| **纯色背景** | 整个页面使用一种颜色作为背景（浅灰/米白/深蓝灰），避免渐变和纹理，突出核心内容 |
| **卡片式布局** | 主要功能模块以卡片形式呈现，有轻微阴影或边框，与背景区分 |
| **高可读性** | 文字颜色与背景形成足够对比，数字/旗帜使用高亮色 |
| **即时反馈** | 任何操作（点击、提交分析）都有明确的视觉/文字反馈 |
| **粘贴即用** | 在对话框中直接粘贴（Ctrl+V / Cmd+V）截图或文本棋盘，系统自动识别并载入 |

---

## 3. 页面布局

采用 **左右结构**（宽屏），分为三个主要区域：

```
┌──────────────────────────────────────────────────────────────────────────┐
│  🧠 扫雷认知助手                              [主题切换] [帮助]        │ ← 导航栏
├──────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌────────────────────────────┐  ┌────────────────────────────────────┐ │
│  │                            │  │  📊 推理结果面板                   │ │
│  │      棋盘编辑器             │  │  · 必雷/安全格高亮                │ │
│  │  (手动点击编辑 / 上传截图)  │  │  · 概率热力图（可选）             │ │
│  │                            │  │  · 推理链文字说明                 │ │
│  │                            │  └────────────────────────────────────┘ │
│  └────────────────────────────┘                                        │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │  💬 对话引导区                                                     │ │
│  │  [输入框: 在此粘贴截图或输入提问...]  [发送]                       │ │
│  │  (支持 Ctrl+V 直接粘贴剪贴板中的截图/文本棋盘)                     │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
│  [ 分析局面 ] [ 教学模式 ] [ 导出结果 ]                                  │ ← 操作栏
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 交互流程

### 4.1 输入局面（三通道）

| 输入方式 | 操作说明 |
|----------|----------|
| **对话框粘贴（推荐）** | 在对话输入框中按 **Ctrl+V / Cmd+V**，直接粘贴截图图像或文本格式的棋盘布局，系统自动识别并载入棋盘 |
| **手动编辑** | 点击棋盘格切换状态（空白 → 数字1-8 → 旗帜 → 未知循环） |
| **截图上传** | 点击“上传截图”按钮，选择图片文件，调用 OCR 服务识别并填充棋盘 |
| **预设用例** | 从“加载示例”下拉菜单快速载入经典残局 |

#### 粘贴识别细则

- **粘贴图像**（来自剪贴板的 PNG/JPEG）：自动调用 OCR 服务（`/api/ocr`），识别结果填入棋盘，并在对话区显示“已识别 N×N 棋盘”
- **粘贴文本**（如 `? ? 1 2 1` 或 `1 2 3` 格式）：按行解析，支持字符映射（`?`=未知，`F`/`🚩`=旗帜，`0-8`=数字），自动构建棋盘

### 4.2 分析触发
- 点击 **“分析局面”** 按钮，前端将当前棋盘数据（PlayerView JSON）发送至后端 API `/analyze`
- 后端返回 IR 数据（推理链、概率、区域划分）

### 4.3 结果展示
- **棋盘覆盖**：在棋盘上以不同颜色高亮标注：
  - 🟢 安全格（绿色边框/半透明绿色覆盖）
  - 🔴 雷格（红色边框/半透明红色覆盖）
  - 🔵 待定格（蓝色数字显示概率百分比）
- **推理链**：在结果面板中以编号列表显示每一步的“因为…所以…”
- **对话区**：显示 LLM 生成的策略建议或教学问题

### 4.4 教学模式
- 开启“教学模式”后，不直接显示结论，而是逐条显示引导性问题
- 用户回答后，系统给出正误判断，并揭示下一步推理
- 可随时关闭教学模式，查看完整答案

### 4.5 策略比较
- 在对话区输入“如果先处理右边区域呢？”，后端返回比较分析
- 结果在对话区以自然语言呈现

---

## 5. UI 组件规范

### 5.1 棋盘格
- **尺寸**：30px × 30px
- **状态样式**：
  - 未翻开：浅灰色填充，无内容
  - 数字：白底，数字颜色遵循经典扫雷配色（蓝/绿/红等）
  - 旗帜：红色旗帜图标（🚩）或自定义 SVG
  - 高亮（分析后）：半透明覆盖层，不影响底层数字显示

### 5.2 按钮
- 风格：圆角矩形，边框无，背景为主色变体（如深灰蓝），白色文字
- 悬停：亮度变化 10%
- 主要按钮：`#2C3E50`（深蓝灰），次要按钮：`#95A5A6`（灰）

### 5.3 对话输入框（支持粘贴）
- 多行文本输入框（`<textarea>`），高度自适应（3-6 行）
- 占位文本：“在此粘贴截图（Ctrl+V）或输入提问...”
- 支持 `paste` 事件监听：
  - 检测剪贴板中的 `image/png`、`image/jpeg` → 自动上传 OCR
  - 检测剪贴板中的 `text/plain` → 自动解析文本棋盘
- 输入框背景与卡片背景一致，边框为浅灰色

### 5.4 卡片
- 背景：白色（或比主背景稍亮的颜色）
- 圆角：8px
- 阴影：`0 2px 8px rgba(0,0,0,0.1)`
- 内边距：16px

### 5.5 文字
- 字体：系统默认无衬线（`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif`）
- 大小：14px（正文），18px（标题），12px（小字）

---

## 6. 颜色方案（纯色背景示例）

| 元素 | 颜色（浅色主题） | 颜色（深色主题） |
|------|----------------|----------------|
| 背景 | `#F0F2F5`（浅灰） | `#1A1A2E`（深蓝黑） |
| 卡片背景 | `#FFFFFF` | `#2D2D44` |
| 主按钮 | `#2C3E50` | `#4A6FA5` |
| 文字主色 | `#333333` | `#EAEAEA` |
| 文字辅助 | `#666666` | `#BBBBBB` |
| 安全高亮（rgba） | `rgba(46, 204, 113, 0.3)` | `rgba(46, 204, 113, 0.4)` |
| 雷高亮（rgba） | `rgba(231, 76, 60, 0.3)` | `rgba(231, 76, 60, 0.4)` |

> 用户可通过界面右上角的“主题切换”在浅色/深色间切换，二者背景均为纯色。

---

## 7. 实现建议

### 7.1 技术选型
- **纯静态 HTML + CSS + JavaScript**（无需框架，便于快速部署）
- CSS 变量管理主题色
- 使用 `fetch` API 与后端通信
- 棋盘渲染使用 `<canvas>`（便于高亮绘制）

### 7.2 状态管理
- 维护 `board[][]` 数组存储每个格子的状态（`'hidden'`, `'number'`, `'flag'`, `'mine'`）
- 分析后增加 `'safe'`, `'mine_high'` 等临时标注状态

### 7.3 粘贴实现（核心）

```javascript
// 监听对话输入框的粘贴事件
inputBox.addEventListener('paste', (e) => {
    const items = e.clipboardData.items;
    
    for (const item of items) {
        // 1. 处理图像粘贴
        if (item.type.startsWith('image/')) {
            const file = item.getAsFile();
            const formData = new FormData();
            formData.append('image', file);
            // 调用 OCR 服务
            fetch('/api/ocr', { method: 'POST', body: formData })
                .then(res => res.json())
                .then(data => loadBoardFromOCR(data));
            break;
        }
        
        // 2. 处理文本粘贴（文本棋盘）
        if (item.type === 'text/plain') {
            item.getAsString(text => {
                const board = parseTextBoard(text);
                if (board) loadBoardFromText(board);
            });
            break;
        }
    }
});
```

---

## 8. 附录：交互细节

### 8.1 棋盘编辑交互
- **左键单击**：循环切换（未知 → 数字1-8 → 未知），数字递增
- **右键单击**：切换旗帜/未知（如需标记雷）
- **Shift + 左键**：快速设为数字0（清除）

### 8.2 分析结果交互
- 点击推理链中的某个步骤，棋盘自动高亮该步骤涉及的格子
- 悬停在高亮格子上，弹出该格子的概率值和依赖信息

### 8.3 对话区交互（含粘贴）
- 支持自由文本输入（用户提问）
- **粘贴触发**：用户按 Ctrl+V 时，自动检测内容类型并路由到对应处理器
- 系统回复以气泡形式展示，区分“系统消息”和“用户消息”
- 支持复制推理链文本
- 输入框支持 Shift+Enter 换行，Enter 直接发送