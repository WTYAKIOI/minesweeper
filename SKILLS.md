### 🧩 核心算法与求解引擎

这部分是 Agent 的“大脑”，负责处理确定性推理和概率模拟。

*   **`ms_toollib`**：一个功能很全的“扫雷算法工具箱”，提供多层次的判雷引擎、统一局面状态机和概率推断引擎。它还支持跨平台（Python/JavaScript）复用，成熟度很高。
*   **`minesweeprs`**：一个优秀的概率求解器 Rust 移植版。它通过抽象的“规则”进行推理，不依赖具体的棋盘几何结构，并能给出每个格子是雷的概率。支持通过 WebAssembly 在浏览器中运行。
*   **`demine`**：另一个正在开发中的扫雷求解器，包含棋盘状态、生成器和求解器等模块，可作为备选或参考。

### 👁️ 图像识别与输入处理

这部分负责将玩家的截图“翻译”成 Agent 能理解的数据。

*   **`opencv-rust`**：Rust 的 OpenCV 绑定。它是截图识别的核心，用于检测棋盘、分割格子。
*   **`tesseract-rs`**：Rust 的 Tesseract OCR 绑定。用于识别格子中的数字，可选内置编译 Tesseract 和 Leptonica 库。
*   **参考项目**：
    *   **`lisudoku_ocr`**：一个数独识别库，思路和你很相似，也是用 OpenCV 分割 + Tesseract 识别数字，可以直接参考。
    *   **`Metasweeper`（元扫雷）**：一个专业的现代化扫雷项目。它内置了“光学局面识别（OBR）引擎”，能从任意扫雷截图重建局面，架构是 **Python/PyQt5 + Rust** 复合模式，非常有参考价值。

### 🧠 LLM 集成层

这部分负责将 Rust 计算出的“中间语言（IR）”翻译成人话，并与用户交互。

*   **`rig`**：一个用于构建可扩展、模块化 LLM 应用的 Rust 库。支持 10+ 种向量存储和完整的 LLM 补全与嵌入工作流，能帮你**用很少的样板代码集成 LLM**。
*   **`llm-kernel`**：一个 Rust 基础库，提供 16 种 LLM 提供商、异步客户端、MCP 服务器等功能。
*   **`helios-engine`**：一个用于构建支持工具的 LLM Agent 的 Rust 框架。

### 🖥️ Web 服务与交互界面

这部分负责将 Agent 包装成可用的服务。

*   **Web 框架**：`actix-web`或 `axum`。用于将你的 Rust 核心逻辑封装成 HTTP API 服务。
*   **终端界面 (TUI)**：`tui-rs`。如果你想先做一个命令行原型，可以参考 `minesweep-rs`或 `mnswpr`等 TUI 扫雷实现。
*   **序列化**：**`serde`** 是 Rust 中序列化/反序列化的事实标准。用于在 Rust 核心与 LLM/前端之间通过 JSON 交换数据。
*   **WebAssembly (WASM)**：`minesweeprs` 支持编译为 WASM 在浏览器中运行。未来可以考虑将核心推理逻辑编译为 WASM，实现纯前端分析。

### 💎 总结与建议

你的技术选型可以遵循一个务实的路径：

1.  **核心算法**：**`ms_toollib`** 功能全面，可作为主力；**`minesweeprs`** 的概率求解器很优秀，可以研究其思路或直接集成。
2.  **图像识别**：直接参考 **`Metasweeper`** 的 OBR 引擎和 **`lisudoku_ocr`** 的实现思路，使用 `opencv-rust` + `tesseract-rs`。
3.  **LLM 集成**：**`rig`** 库设计现代，能帮你写出更干净的集成代码。
4.  **服务接口**：先用 **`actix-web`** 或 **`axum`** 搭建 HTTP API，方便调试和前端联调。
5.  **参考项目**：**`Metasweeper`** 与你设想的部分功能高度重合，它的复合架构和 OBR 引擎是很好的学习范本。

建议先从核心算法和图像识别这两个关键模块开始验证，把推理链路跑通，再逐步集成 LLM 和 Web 服务。祝开发顺利～