我将为你提供一份完整的 `AGENT.md` 文档，并附带详细的开发实施步骤。这份文档将作为你进行 Vibe Coding（在 AI 辅助下快速迭代开发）的蓝图。

---

# AGENT.md – 扫雷认知助手 (Minesweeper Cognitive Agent)

## 1. 项目概述

**扫雷认知助手** 是一个专为扫雷玩家设计的智能分析工具。它通过截图/牌谱重建局面，结合 **确定性推理（Rust）** 与 **大语言模型（LLM）** 的战略转译能力，为玩家提供：

- 带完整推理链的落子建议（必雷/必安全）
- 非确定局面下的概率评估与收益分析
- 引导式教学模式（不直接给答案，而是提问）
- 全局策略比较（先处理哪片区域更优）

**核心理念**：Rust 负责绝对理性的数学计算，LLM 负责将计算中间语言翻译为人类可理解的策略语言，并确保 **“数据防火墙”** —— 任何传给 LLM 的信息都不包含未翻开格子的真实雷藏，杜绝马后炮。

---

## 2. 整体架构（分层解耦）

```
┌─────────────────────────────────────────────────────────┐
│                    用户交互层 (Frontend)                  │
│  - 截图上传 / 牌谱导入                                   │
│  - 棋盘手动修正工具                                      │
│  - 推理高亮展示 & 对话窗口                               │
└─────────────────────────┬───────────────────────────────┘
                          │ (OCR/图像识别结果)
                          ▼
┌─────────────────────────────────────────────────────────┐
│               数据清洗 & PlayerView 重建 (Rust)           │
│  - 输入验证与归一化                                      │
│  - 构造 PlayerView (只含已翻开数字、旗帜、未知坐标)       │
│  - 合法性检查（数字约束自洽）                            │
└─────────────────────────┬───────────────────────────────┘
                          │ (PlayerView)
                          ▼
┌─────────────────────────────────────────────────────────┐
│              双引擎推理核心 (Rust)                        │
│  ├─ 确定性约束传播 → 推理链 DAG + 必雷/安全格            │
│  └─ 蒙特卡洛模拟     → 每个格子的暴雷概率 + 期望信息增益  │
└─────────────────────────┬───────────────────────────────┘
                          │ (中间语言 IR - JSON)
                          ▼
┌─────────────────────────────────────────────────────────┐
│              LLM 战略转译层 (Python/OpenAI API)           │
│  - 推理链 → 自然语言（含坐标映射）                       │
│  - 概率/收益 → 战术解读（定性分析）                      │
│  - 教学模式 → 逆推理链生成引导问题                       │
│  - 全局策略比较 → 区域利弊分析                          │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
                   前端展示 & 对话
```

---

## 3. 技术栈选择

| 模块 | 技术 |
|------|------|
| 截图识别 | OpenCV (Rust 绑定 `opencv-rust`) + Tesseract OCR (或 `tesseract-rs`) |
| 数据/推理核心 | **Rust** (高性能、内存安全) |
| 概率模拟 | Rust 随机数库 (`rand`) 或 精确组合枚举 |
| LLM 交互 | Python (FastAPI) 或 Rust (通过 `reqwest` 调用 OpenAI API) |
| 前端原型 | 简单 Web 界面 (HTML+JS) 或 TUI (终端交互) |
| 通信协议 | JSON over HTTP (Rust 提供 Web API) |

> **注**：为快速起步，你可以先使用 Rust 构建 CLI 工具，后期再封装为 Web 服务。

---

## 4. 模块职责与接口定义

### 4.1 PlayerView（Rust 数据结构）

```rust
pub struct PlayerView {
    pub width: u32,
    pub height: u32,
    pub revealed: Vec<RevealedCell>,   // 坐标 + 数字 (1-8)
    pub flagged: Vec<Coord>,           // 玩家已标旗的位置
    pub unknown: Vec<Coord>,           // 未翻开且未标旗的位置
    pub remaining_mines: u32,          // 剩余总雷数
}

pub struct RevealedCell {
    pub coord: Coord,
    pub number: u8,
}
```

### 4.2 推理引擎输出 IR（中间语言）

```rust
pub struct InferenceIR {
    pub deterministic: Vec<Proof>,     // 每个证明包含结论 (雷/安全), 依赖的格子列表
    pub probabilities: Vec<CellProb>,  // 坐标 + 雷概率 + 信息增益
    pub regions: Vec<Region>,          // 连通未翻开区域（用于策略比较）
}

pub struct Proof {
    pub conclusion: Conclusion,        // {Coord, is_mine}
    pub depends_on: Vec<Coord>,        // 推理依赖的已翻开数字坐标
    pub rule: String,                 // 规则简述，如 "数字3周围已有2旗"
}
```

### 4.3 LLM 调用接口

Rust 将 `InferenceIR` 转为 JSON，通过 HTTP 请求发送给 LLM 服务（或用 Python 包装），并要求 LLM 返回结构化回答（或自由文本，但需严格遵守坐标引用）。

---

## 5. 开发实施步骤（逐步实现）

以下步骤按 **最小可行产品 (MVP)** 迭代规划，每步都有可验证的产出。

### Phase 0: 环境准备与基础设施（1天）
- [ ] 安装 Rust、Cargo，创建新项目 `minesweeper-agent`
- [ ] 添加依赖：`serde` (序列化), `rand` (随机), `image` (图像处理), `tesseract-rs` 等
- [ ] 设计并实现基础数据结构 `PlayerView`, `Coord`, `InferenceIR`（先空实现）
- [ ] 编写简单的单元测试框架

### Phase 1: 截图识别与 PlayerView 重建（3天）
- [ ] 使用 OpenCV 从截图检测棋盘边框，切割出每个格子
- [ ] 实现模板匹配识别“未翻开”、“旗帜”、“数字”三种状态
- [ ] 对数字区域调用 Tesseract OCR 识别 1-8
- [ ] 组装成 `PlayerView`，并输出 JSON 以供调试
- [ ] **验证**：用已知截图测试，手动比对重建结果

### Phase 2: 确定性推理引擎（核心）（5天）
- [ ] 实现约束传播算法（类似数独）：  
  - 对每个已翻开数字，计算其周围 `未知格数` 与 `剩余雷数`  
  - 若未知格数 == 剩余雷数 → 全部标雷  
  - 若剩余雷数 == 0 → 全部标安全
- [ ] 记录推理依赖链（每个结论依赖哪些数字），生成 `Proof` 结构
- [ ] 实现循环推理，直到不再有新结论
- [ ] **验证**：用已知残局测试，确保输出所有必雷/安全格，且推理链完整

### Phase 3: 概率模拟引擎（3天）
- [ ] 实现蒙特卡洛模拟：对剩余未知格子进行多次随机分配雷，统计每个格子被踩中的频率
- [ ] 计算期望信息增益（模拟点击后，若为0会连锁展开的面积）
- [ ] 输出每个格子的 `(雷率, 信息增益)` 给 IR
- [ ] **验证**：与已知概率算法（如组合枚举）在小棋盘上对照，误差在可接受范围

### Phase 4: LLM 集成与转译（5天）
- [ ] 选择 LLM API（如 OpenAI GPT-4o-mini），用 Python 或 Rust 实现调用
- [ ] 设计 System Prompt，要求模型严格遵循 IR 中的数据，不得编造坐标
- [ ] 实现三种模式：
  - **答案模式**：将证明链转为流畅的“因为...所以...”文字
  - **教学模式**：将证明链倒置，生成逐步引导问题（如“数字3周围已有几旗？”）
  - **策略模式**：基于概率+收益数据，生成区域比较建议
- [ ] **验证**：用几个典型局面测试，检查输出是否包含正确坐标，是否合乎逻辑

### Phase 5: 交互前端（2天）
- [ ] 开发简易 Web 界面（或 CLI 菜单）
- [ ] 用户上传截图 → 调用识别模块 → 显示重建棋盘
- [ ] 用户点击“分析” → 调用推理 → 显示结果（文字+高亮）
- [ ] 支持教学/专家模式切换

### Phase 6: 全局策略比较（附加）（2天）
- [ ] 在 Rust 中实现连通区域划分（并查集）
- [ ] 对每个区域计算“数字密度”、“边界长度”等特征
- [ ] LLM 根据这些特征生成“先左还是先右”的利弊分析
- [ ] **验证**：人工评判分析的合理性

### Phase 7: 测试、优化与文档（2天）
- [ ] 集成测试：端到端测试多个真实游戏截图
- [ ] 性能优化：Rust 计算部分需在毫秒级
- [ ] 完善 `AGENT.md` 与用户手册

---

## 6. 测试策略

- **单元测试**：对每个 Rust 模块（约束传播、概率模拟）进行数据驱动测试，使用固定局面验证输出。
- **截图识别测试**：收集至少 20 张不同扫雷游戏截图（Windows、网页、手机），人工标注结果，计算识别准确率。
- **端到端测试**：模拟玩家对局，检查 Agent 是否给出合理建议，且不泄露雷藏信息。
- **LLM 输出质量评估**：人工检查生成文本是否忠实于 IR，是否有坐标越界等幻觉。

---

## 7. 部署与扩展

- **初期**：提供 Web 服务（Rust + Actix-web）或直接命令行工具。
- **后期**：可封装为浏览器插件，实时捕获游戏画面。
- **扩展点**：支持更多扫雷变体（如六边形扫雷），或接入强化学习优化策略。

---

## 8. 开发者注意事项

- **数据防火墙原则**：任何 API 调用或 LLM 输入，严禁包含未翻开格子的真实雷藏。所有雷信息仅存在于 Rust 模拟过程中，不输出给外部。
- **日志记录**：记录每次推理的 IR 和 LLM 响应，便于调试与改进提示词。
- **版本控制**：每个 Phase 完成后打 tag，方便回退。

---

## 9. 后续迭代方向

- 引入“推理链可视化”在前端以图形化方式展示依赖关系。
- 支持导入完整牌谱（如从扫雷记录网站）进行复盘分析。
- 集成语音交互，用于竞速场景。

---

**开始 Vibe Coding！** 请从 Phase 0 开始，按阶段逐步实现，每完成一个阶段进行验证。若有疑问，可参考开源项目（如 `minesweeper_solver`）的实现细节，但需注意重写为 Rust 并融入 LLM 层。祝开发顺利！

注意标注出引用的开源库。